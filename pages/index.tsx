import type { NextPage } from "next";
import { Box } from "@chakra-ui/react";
import { Layout } from "../components";

const Home: NextPage = () => {
  return (
    <Layout>
      <Box></Box>
    </Layout>
  );
};

export default Home;
