import { Box, Typography } from "@mui/material";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <Box>
      <Typography>Gmail clone!</Typography>
    </Box>
  );
};

export default Home;
